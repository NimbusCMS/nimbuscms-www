# Seeding target (the site's MCP endpoint) + a scoped token.
# Mint on the target: nimbus token:create --name=www-seed \
#   --scopes=schema:write,settings:write,*:read,*:write
NIMBUS_MCP_URL=https://nimbuscms.dev/api/v1/mcp
NIMBUS_MCP_TOKEN=
