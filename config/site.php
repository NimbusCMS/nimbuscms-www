<?php

declare(strict_types=1);

/** Site-wide defaults (used where a page supplies none of its own). */
return [
    'description' => 'A lightweight PHP CMS an agent can run — and a human can read.',
];
